# Shared defaults, committed. Override per machine in ~/.gradle/gradle.properties
# (%USERPROFILE%\.gradle\gradle.properties on Windows) or with -P on the command line;
# see docs/README-getting-started.md.

# Base URL of the proxy API. 10.0.2.2 is the Android emulator's alias for the host's
# localhost, which WSL2 forwards to the API running inside Linux.
jsonplaceholder.apiUrl=http://10.0.2.2:3000

org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.parallel=true

android.useAndroidX=true
android.nonTransitiveRClass=true
android.builtInKotlin=false
android.newDsl=false
kotlin.code.style=official

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
